---
import { Image } from "astro:assets";
const { src, alt, style } = Astro.props;
---

<div
  class={`w-full max-w-[300px] h-full max-h-[400px] px-4 rounded-t-md ${style}`}
>
  <Image
    src={src}
    alt={alt}
    class="w-full max-w-[240px] object-cover mx-auto"
  />
  <slot />
</div>
