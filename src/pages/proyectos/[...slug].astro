---
import MainLayout from "@layouts/MainLayout.astro";
import { DetailProjectLayout } from "@layouts/DetailProjectLayout.jsx";
import { getPathsProjects } from "@api/apiWordpress";

export async function getStaticPaths() {
  const paths = await getPathsProjects();
  return paths;
}

const { slug } = Astro.params;
---

<MainLayout title={`Proyecto ${slug}`} description={`Conoce más detalles del proyecto de Diprofire: ${slug}`}>
  <DetailProjectLayout slug={slug} client:only />
</MainLayout>
