---
import MainLayout from "@layouts/MainLayout.astro";
import { DetailProjectLayout } from "@layouts/DetailProjectLayout.jsx";
export const prerender = false;

const { slug } = Astro.params;

const parsedSlug = slug?.replace(/-/g, " ");
---

<MainLayout title={`Proyecto ${parsedSlug}`} description={`Conoce más detalles del proyecto de Diprofire: ${parsedSlug}`}>
  <DetailProjectLayout slug={slug} client:only />
</MainLayout>
